import type { NextConfig } from "next";

export const nextConfig: NextConfig = {
  /* config options here */
};

export const images = {
  domains: [
    "images.unsplash.com"
  ],
};

// export default nextConfig;
